// describe('Chain crash test', function () {
// 	it('should NOT CRASH on chain test!', function () {
// 		var ch = chain([1, 2, 3]);
// 		ch.map(x=> x * 2);
// 		expect(ch.value()).to.be.eql([1, 2, 3]);
// 		var ch2 = ch.map(x=> x * 2);
// 		expect(ch.value()).to.be.eql([1, 2, 3]);
// 		expect(ch2.value()).to.be.eql([2, 4, 6]);
// 	});
// });